## 1. 顶部工具栏结构

- [x] 1.1 梳理 `TopToolbar.tsx` 中现有入口与对应 ViewModel action，确认聚合前后 action、loading 和 disabled 语义不变。
- [x] 1.2 将顶部工具栏拆成行情查询区、视图参数区、主操作区、当前证券标题区和 `更多` 菜单区。
- [x] 1.3 保持证券代码输入、自选股入口、分时/K 线切换、当前模式查询参数、刷新和当前证券标题直接可见。
- [x] 1.4 将 `数据源`、`代理`、`导出缓存`、`导入缓存`、`检查更新` 和 `启动检查更新` 收入 `更多` 菜单，并保持触发方法与状态反馈不变。
- [x] 1.5 保持 `指标` 和 `做T` 顶部直接可见；`策略` 仅在 K 线模式下直接展示，分时模式不展示可触发入口。

## 2. 样式和响应式

- [x] 2.1 调整 `.top-toolbar` 和新增分组样式，使默认桌面窗口宽度下不依赖横向滚动展示主要操作。
- [x] 2.2 为当前证券标题、K 线日期输入、工具按钮和菜单按钮设置稳定宽度或响应式约束，避免文本挤压或布局跳动。
- [x] 2.3 增加窄窗口样式，优先保留查询输入、视图切换、刷新和聚合菜单可用，次级工具压缩为 icon 或菜单入口。

## 3. 测试

- [x] 3.1 更新 toolbar 相关单测，验证主操作仍直接可见并绑定原有 action。
- [x] 3.2 新增或调整菜单项测试，覆盖数据源、代理、本地缓存导入导出、检查更新和启动检查更新开关。
- [x] 3.3 覆盖 K 线模式下 `策略` 入口可用、分时模式下不展示或禁用 `策略` 入口。
- [x] 3.4 更新因按钮从静态顶部区域移动到菜单后失效的文档或缓存导入导出测试。

## 4. 文档和变更记录

- [x] 4.1 更新 `README.md` 中顶部工具栏相关入口描述。
- [x] 4.2 更新应用内 `使用说明书` 中数据源、代理、缓存导入导出、检查更新、指标、做T 和策略入口位置描述。
- [x] 4.3 更新 `CHANGELOG.md` 的 `Unreleased / <current package.json version>` 区块，记录顶部菜单 UI 聚合优化。

## 5. 验证

- [x] 5.1 运行 `openspec validate optimize-top-menu-ui --strict`。
- [x] 5.2 运行 toolbar、文档和本地缓存导入导出相关测试。
- [x] 5.3 运行 `yarn typecheck` 和 `yarn test`。
