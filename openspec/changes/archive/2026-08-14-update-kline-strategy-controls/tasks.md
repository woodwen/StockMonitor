## 1. K 线指标模型和互斥规则

- [x] 1.1 在 K 线指标类型和定义中新增 `strategySignal` overlay 指标，用户可见标签为 `策略`，默认关闭且无参数。
- [x] 1.2 在 K 线指标 normalize 中补齐旧设置缺失的 `strategySignal` 为关闭，并保证异常设置同时开启 `bsSignal` 与 `strategySignal` 时只保留 `bsSignal`。
- [x] 1.3 在 K 线指标草稿和直接切换路径中实现最新操作优先的互斥：开启 `strategySignal` 时关闭 `bsSignal`，开启 `bsSignal` 时关闭 `strategySignal`。
- [x] 1.4 更新 K 线指标 model 单测，覆盖默认值、旧配置迁移、异常双开 normalize、草稿互斥和应用/取消行为。

## 2. 策略回测区间和图表信号编排

- [x] 2.1 从 `KlineStrategySettings` 运行态设置中移除独立 `dateRange`，保留旧字段输入兼容但 normalize 后不再写回。
- [x] 2.2 删除策略回测设置中的开始日期和结束日期输入，改为只读展示当前 K 线 query 的日期范围，并提示通过外部 K 线日期范围调整；最大回撤指标算法和展示口径保持不变。
- [x] 2.3 将策略回测 query 组装改为使用当前 K 线 query 的 `sourceId`、`symbol`、`period`、`adjust`、`startDate` 和 `endDate`。
- [x] 2.4 调整 ViewModel 表单校验、运行状态和持久化逻辑，移除独立策略日期输入、setter 和旧日期错误路径。
- [x] 2.5 让图表策略标记仅在 `strategySignal` 开启且选中成功策略结果存在时同步；关闭 `strategySignal` 或开启 `bsSignal` 时清空策略 overlay。
- [x] 2.6 更新 KlineCharts adapter 或调用侧测试，确认 B/S overlay 与 strategy overlay 不会同时显示，关闭策略开关时会移除 strategy overlay。
- [x] 2.7 修复完整缓存首尾遇到非交易日时策略回测误报缺失区间的问题，同时保留大范围实际缺失的阻断逻辑。

## 3. 历史 K 线缓存列表

- [x] 3.1 在历史 K 线缓存弹窗表格中新增 `数据源` 列，默认放在 `名称` 后、`周期` 前，列宽 96px。
- [x] 3.2 基于每行 `query.sourceId` 映射并展示数据源显示名，例如 `东方财富`，而不是只使用顶部当前数据源选择值。
- [x] 3.3 对未知数据源显示原始 `sourceId`，避免数据源列为空白。
- [x] 3.4 调整表格宽度、横向滚动或列宽，确保新增列后名称、周期、复权、状态、缓存范围和最近刷新等信息不互相遮挡。
- [x] 3.5 保持历史 K 线缓存列表既有交互能力不变，不新增数据源筛选、排序或分组。
- [x] 3.6 更新历史 K 线缓存弹窗相关测试，覆盖 `数据源` 列标题、96px 默认宽度、东方财富等显示名、未知源 fallback 和每行 query 映射。

## 4. 用户文档和变更记录

- [x] 4.1 更新 `README.md`，说明 K 线指标中的 `B/S` 与 `策略` 信号开关互斥、`策略` 默认关闭、策略回测日期范围来自顶部 K 线设置，以及历史 K 线缓存列表展示数据源列。
- [x] 4.2 更新应用内 `使用说明书`，同步指标开关默认状态、策略回测区间来源、历史 K 线缓存数据源列和高风险功能安全文案。
- [x] 4.3 更新 `CHANGELOG.md` 的 `Unreleased / <current package.json version>` 区块，记录本次用户可见行为和文档变更。

## 5. 验证

- [x] 5.1 运行 `openspec validate update-kline-strategy-controls --strict`，确保规划 artifacts 合法。
- [x] 5.2 运行 `yarn test tests/stock-workspace-view-model.test.ts`，覆盖策略区间来源、互斥开关和策略信号同步。
- [x] 5.3 运行相关 K 线指标、策略回测、KlineCharts adapter、历史 K 线缓存弹窗和文档测试。
- [x] 5.4 运行 `yarn typecheck` 和 `yarn test` 作为普通代码改动总体验证。
- [x] 5.5 增加并运行完整缓存非交易日边界回归测试，覆盖 `20240810-20240811` 这类误报缺失场景。

## 6. 历史 K 线缓存模式和默认选择追加需求

- [x] 6.1 在历史 K 线缓存弹窗顶部新增缓存对象模式控件，区分 `单只股票缓存` 与 `多只股票缓存`；每次打开弹窗默认使用 `单只股票缓存`，不记忆上次模式。
- [x] 6.2 单只股票缓存模式下，缓存状态、刷新和清理请求只使用当前 K 线 query 的 `symbol` 与当前证券显示名称构造 items；即使当前证券不在自选股列表中也允许缓存。
- [x] 6.3 多只股票缓存模式下，缓存状态、刷新和清理请求使用当前自选股列表构造 items；自选股为空时展示空状态、禁用刷新，并避免提交空 items 的批量缓存刷新任务。
- [x] 6.4 切换 `单只股票缓存` / `多只股票缓存` 时，保持当前数据源、日期范围、周期选择和复权选择不变，清空缓存表格选中项并重新查询缓存状态。
- [x] 6.5 将 `刷新全部` 语义绑定到当前缓存对象模式：单只模式只刷新当前证券，多只模式刷新当前自选股列表。
- [x] 6.6 将历史 K 线缓存弹窗默认勾选项调整为仅勾选 `日线` 和 `前复权`，`周线`、`月线`、`不复权`、`后复权` 默认不勾选，同时保留用户手动勾选能力。
- [x] 6.7 更新历史 K 线缓存弹窗 UI 和测试，覆盖顶部模式控件、每次打开默认单只且不记忆模式、当前证券不在自选股时可缓存、多只空列表禁用刷新、切换模式清选中并重新查询、默认日线前复权、其他选项默认不勾选，以及请求 items 来源。
- [x] 6.8 更新 `README.md`、应用内 `使用说明书` 和 `CHANGELOG.md`，说明历史 K 线缓存默认单只股票缓存、不记忆上次模式、可切换多只股票缓存、`刷新全部` 的模式语义，以及默认只勾选日线前复权。
- [x] 6.9 运行 `openspec validate update-kline-strategy-controls --strict`、`openspec validate --all --strict`、历史 K 线缓存弹窗相关测试、`yarn typecheck`、`yarn test` 和 `git diff --check`。
