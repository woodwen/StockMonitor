## 1. 策略模型与回测核心

- [x] 1.1 在 `stock-workspace/models` 中新增策略模板、参数、信号、交易、权益曲线、回测指标和排名结果类型。
- [x] 1.2 实现策略模板 registry，提供均线交叉、突破回撤、RSI 超买超卖和 MACD 趋势确认模板的默认参数、参数约束、最小样本数，并将兼容周期限定为 `day`、`week`、`month`。
- [x] 1.3 实现策略参数 normalize 与校验逻辑，覆盖无效周期、分钟周期暂不支持、无效阈值、短周期大于长周期、样本不足等错误状态。
- [x] 1.4 实现策略所需的纯函数指标计算和买入/卖出信号生成，保证同一 candles、模板和参数输出确定性一致。
- [x] 1.5 实现多头、全仓、单笔持仓回测引擎，按下一根 K 线开盘价成交并处理期末未平仓估值。
- [x] 1.6 实现总收益率、年化收益率、最大回撤、胜率、交易次数、盈亏比、平均持仓 K 线数、期末权益、基准涨跌幅和 `returnDrawdownRatio` 排名计算。
- [x] 1.7 为策略模板、参数校验、分钟周期不可运行、信号生成、交易撮合、最大回撤、无交易结果和多策略排名添加 focused unit tests。

## 2. 工作区状态与数据读取

- [x] 2.1 扩展 `WorkspaceSettings` 和 store normalize，持久化兼容的策略模板选择、参数和回测假设，旧设置缺失字段时补齐默认值。
- [x] 2.2 在 ViewModel 中新增策略面板打开状态、参数草稿、回测假设、运行状态、错误状态、结果列表和选中结果状态。
- [x] 2.3 实现回测数据解析逻辑：通过 `StockDataAdapter.getCachedKlineDataset(query)` 读取完整本地缓存，当前图表 dataset 不作为缓存缺失时的替代输入。
- [x] 2.4 在历史数据缺失、缓存不完整、模板不可用、参数无效或计算失败时展示独立策略错误，并保持行情加载状态和缓存任务状态不变。
- [x] 2.5 在 symbol、数据源、周期、复权或日期范围变化时清空或标记旧策略结果过期，避免旧信号继续渲染到新图表。
- [x] 2.6 为策略状态初始化、偏好持久化、日/周/月运行成功、分钟周期阻止回测、失败隔离、query 变化清理和分时模式不自动回测添加 `tests/stock-workspace-view-model.test.ts` 覆盖。
- [x] 2.7 增加策略回测区间草稿、校验、持久化和数据读取编排，默认使用当前 K 线 query 日期范围，运行时不改变图表 query。
- [x] 2.8 为策略回测区间选择、无效日期阻止回测、本地缓存完整执行路径和缓存 fallback 添加 ViewModel 测试。
- [x] 2.9 调整策略回测数据准备流程：优先读取本地 K 线缓存；缓存缺失或不完整时触发对应 query 的缓存下载/刷新，刷新完成后再读取缓存执行回测。
- [x] 2.10 为策略触发缓存下载、缓存刷新成功后自动回测、缓存刷新失败展示错误、缓存任务状态隔离和 renderer 不直连远端行情添加测试覆盖。
- [x] 2.11 在策略数据准备中二次校验 cached dataset 的实际 candles 起止范围；刷新后仍不覆盖所选回测区间时阻止回测并展示缺失范围。
- [x] 2.12 将单个模板参数错误限定为对应策略结果不可用，避免阻塞同批其他有效模板回测。
- [x] 2.13 在策略缓存准备轮询中支持 query 变化、视图切换或销毁后的请求失效中止，并在缓存刷新失败信息中保留缺失范围。

## 3. K 线策略 UI 与图表展示

- [x] 3.1 在 K 线工作区新增策略回测面板入口，默认使用当前 K 线 query、策略默认参数和默认回测假设。
- [x] 3.2 构建策略模板选择、参数编辑、初始资金、费用率、滑点率和运行按钮，所有输入控件展示校验状态。
- [x] 3.3 展示多策略历史回测排名、收益/回撤摘要、不可用原因、交易明细、信号说明和回测假设。
- [x] 3.4 扩展 KLineCharts adapter，使其接收已计算的策略信号并渲染买入/卖出 overlay，且不在 adapter 内重新计算策略逻辑。
- [x] 3.5 校对策略面板用户可见文案，确保使用历史回测、候选策略、信号和表现排名等描述，不出现投资建议、买卖建议、荐股或收益承诺。
- [x] 3.6 在策略回测面板中展示开始日期和结束日期输入，并与表单错误、运行按钮状态联动。
- [x] 3.7 在策略回测面板中用运行按钮 loading 和简短状态文本展示历史 K 线缓存准备状态，区分策略运行、缓存下载和回测失败信息。
- [x] 3.8 在策略结果说明中区分用户选择的回测区间和实际参与计算的数据区间，避免可用 K 线起点晚于选择日期时产生误解。
- [x] 3.9 调整 K 线图表策略 B/S 标记配色和描边，使买入与卖出信号在深色背景上明显区分。
- [x] 3.10 在选中策略摘要中固定展示回测区间和实际数据区间。

## 4. 文档与回归验证

- [x] 4.1 更新 `CHANGELOG.md` 的 `Unreleased / 0.1.7` 区块，记录 K 线策略回测与历史表现排名功能。
- [x] 4.2 运行 `yarn typecheck`，修复类型错误。
- [x] 4.3 运行 `yarn test` 和 `yarn test tests/stock-workspace-view-model.test.ts`，修复回归失败。
- [x] 4.4 如实施中新增或修改 `StockApi`、preload 或 main IPC，同步更新 adapter 与 `tests/ipc-handlers.test.ts`，并运行对应 IPC 测试。
- [x] 4.5 如实施中修改 main process、preload、Electron 配置或打包相关文件，运行 `yarn build`。
- [x] 4.6 运行 `openspec validate add-kline-strategy-backtest-recommendations --strict`，确认 change artifacts 通过严格校验。
- [x] 4.7 补充策略模型无效 K 线输入、非 MA 模板正向路径、混合有效/无效模板和过期策略轮询测试。
