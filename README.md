# Stock Monitor

Electron + React + MobX MVVM K-line desktop application.

## Scripts

- `yarn dev` starts the desktop app in development mode.
- `yarn test` runs model and ViewModel tests.
- `yarn typecheck` runs TypeScript checks.
- `yarn dist` builds release artifacts with electron-builder.

The bundled sample dataset lives at `fixtures/legacy/000002.txt`.
